#!/usr/bin/env python3
"""Repository-local launcher for bob-der."""

from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parent / "src"))

from bob_der.cli import main  # noqa: E402


if __name__ == "__main__":
    raise SystemExit(main())
